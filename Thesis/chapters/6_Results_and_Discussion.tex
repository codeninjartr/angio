\chapter{Results and Discussion}
\label{ch:results_and_discussion}

This chapter presents an exhaustive empirical analysis of the experimental results obtained by deploying the proposed UNet++ and DeepLabV3+ architectures on the 637-image clinical angiography dataset. The evaluation is systematically partitioned into a quantitative analysis of semantic segmentation metrics, a qualitative visual assessment of the predicted probability maps, and a quantitative discussion of the downstream morphological biomarkers extracted via the automated skeletonization pipeline.

\section{Quantitative Semantic Segmentation Results}
The fundamental objective of the primary deep learning phase is to maximize the spatial overlap between the predicted binary vessel masks and the expert-annotated ground truth. This spatial fidelity was rigorously evaluated on the independent, 137-image hold-out testing set using the Intersection over Union (IoU) and the Dice Coefficient.

\subsection{Convergence and Learning Dynamics}
The training of all models was governed by the hybrid Binary Cross-Entropy and Dice Loss ($\mathcal{L}_{BCE+Dice}$) formulation. As theoretically anticipated in Chapter 3, the inclusion of the regional Dice loss component successfully prevented the networks from collapsing into a trivial all-background prediction state---a notorious failure mode for highly imbalanced angiographic data.

Analysis of the learning curves reveals distinct convergence behaviors between the architectures. The \textbf{DeepLabV3+} architecture, leveraging its ImageNet-pretrained ResNet50 backbone, demonstrated remarkably rapid early convergence. Within the first 10 epochs, DeepLabV3+ achieved a validation IoU exceeding 0.84. However, the performance plateaued shortly thereafter, triggering the \texttt{ReduceLROnPlateau} scheduler to decay the learning rate from $1 \times 10^{-4}$ to $2 \times 10^{-5}$. The model ultimately converged at epoch 14, where the \texttt{EarlyStopping} callback terminated training to prevent overfitting, restoring the optimal weights from epoch 13 which yielded a validation loss of $0.4714$.

Conversely, the \textbf{UNet++ (Scratch)} architecture exhibited a much slower, yet highly stable, initial learning phase. Without the benefit of pre-trained weights, the dense skip pathways required significantly more iterations to initialize feature detectors capable of isolating low-contrast micro-capillaries. However, the \textbf{UNet++ (ResNet50)} variant combined the rapid early convergence of the pre-trained backbone with the deep feature aggregation of the dense decoder, resulting in the most stable overall learning trajectory with the lowest variance in validation loss across the final 15 epochs.

\begin{figure}[htbp]
\centering
\includegraphics[width=\textwidth]{images/training_metrics_plot.png}
\caption{Model training and validation curves demonstrating convergence behavior across epochs. Left: Hybrid Loss (BCE + Dice Combined) shows smooth, stable minimization. Middle: Dice Coefficient (F1-score) tracking the overlap of predicted vessels. Right: Mean Intersection over Union (IoU) scores confirming test set segmentation fidelity.}
\label{fig:training_curves}
\end{figure}

The detailed, per-architecture training dynamics for both models are presented in Figures~\ref{fig:unetpp_training_detail} and \ref{fig:deeplabv3p_training_detail}. These comprehensive 2$\times$2 training curve panels visualize the evolution of loss, Dice coefficient, IoU, and additional architecture-specific metrics across training epochs.

\begin{figure}[H]
\centering
\includegraphics[width=0.92\textwidth]{images/unetpp_training_curve.png}
\caption{Detailed UNet++ (ResNet50) training dynamics. Top-left: Training and validation loss (BCE + Dice). Top-right: Dice coefficient progression. Bottom-left: Intersection over Union (IoU). Bottom-right: Precision and Recall curves, showing balanced optimization without class collapse.}
\label{fig:unetpp_training_detail}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.92\textwidth]{images/deeplabv3plus_training_curve.png}
\caption{Detailed DeepLabV3+ training dynamics. Top-left: Training and validation loss (BCE + Dice). Top-right: Dice coefficient progression. Bottom-left: Intersection over Union (IoU). Bottom-right: Pixel-wise accuracy, demonstrating rapid early convergence due to the pre-trained ResNet50 backbone.}
\label{fig:deeplabv3p_training_detail}
\end{figure}

\subsection{Comparative Analysis of IoU and Dice Metrics}
The final quantitative metrics evaluated on the 137-image testing set unequivocally demonstrate the high efficacy of both advanced architectures.

\begin{table}[h]
\centering
\caption{Quantitative Comparison of Segmentation Architectures on the 137-Image Testing Set}
\label{tab:quantitative_results}
\begin{tabular}{@{}lcccc@{}}
\toprule
\textbf{Architecture} & \textbf{Loss Function} & \textbf{Pixel Acc.} & \textbf{Val. IoU} & \textbf{Val. Dice} \\ \midrule
UNet (Baseline) & BCE & 0.9412 & 0.7645 & 0.8665 \\
UNet++ (Scratch) & BCE + Dice & 0.9588 & 0.8210 & 0.9017 \\
UNet++ (ResNet50) & BCE + Dice & \textbf{0.9631} & \textbf{0.8605} & \textbf{0.9250} \\
DeepLabV3+ & BCE + Dice & 0.9590 & 0.8547 & 0.9216 \\ \bottomrule
\end{tabular}
\end{table}

As detailed in Table \ref{tab:quantitative_results}, standard U-Net optimized solely on BCE achieves a deceptively high global pixel accuracy ($0.9412$) while severely underperforming on the critical Intersection over Union metric ($0.7645$). This massive discrepancy confirms the severe class imbalance problem discussed in Section 1.2; the high accuracy is entirely driven by the correct classification of the vast background regions, while the structural overlap of the actual vessels remains poor.

The transition to \textbf{UNet++ (Scratch)} utilizing the hybrid $\mathcal{L}_{BCE+Dice}$ immediately yields a massive $+0.0565$ absolute improvement in Validation IoU. This empirical result directly validates Research Question 1 (RQ1) and Research Question 2 (RQ2): the dense, nested skip pathways allow the decoder to recover high-resolution spatial continuity, and the Dice loss component successfully forces the optimizer to prioritize foreground overlap.

Both pre-trained architectures perform exceptionally well. \textbf{DeepLabV3+} achieves a stellar Validation IoU of $0.8547$ and a Validation Dice of $0.9216$. The Atrous Spatial Pyramid Pooling (ASPP) module effectively captures the multi-scale contextual information required to segment both the primary thick arteries and the thinner branching veins.

However, the highest overall performance is achieved by \textbf{UNet++ (ResNet50)}, peaking at a Validation IoU of $0.8605$ and a Dice Coefficient of $0.9250$. The empirical evidence suggests that while DeepLabV3+'s ASPP is highly effective, the dense semantic aggregation occurring across the $X_{i,j}$ grid of UNet++ provides a slight, but measurable, advantage in preserving the absolute finest, single-pixel-wide micro-capillaries at the extreme periphery of the angiograms.

\section{Qualitative Visual Assessment}
While numerical metrics like IoU provide a global statistical summary, the clinical utility of a segmentation model is ultimately judged by the visual continuity and structural integrity of the predicted probability maps. Qualitative analysis was performed by generating prediction overlays for the testing set (e.g., \texttt{overlay\_501\_RGB.jpg.png} through \texttt{overlay\_510\_RGB.jpg.png}).

\subsection{Resolution of Thick vs. Thin Vessels}
Visual inspection of the DeepLabV3+ prediction overlays confirms the numerical data. The model displays near-perfect precision when segmenting the primary, thick vascular trunks located centrally in the images. The boundaries of these thick vessels are predicted with exceptionally high probability ($P \rightarrow 1.0$), resulting in sharp, smooth edges devoid of jagged artifacts.

The true test of these architectures lies in the peripheral regions where vessels bifurcate into ultra-thin capillaries. Both UNet++ and DeepLabV3+ successfully detect the vast majority of these structures. However, qualitative differences emerge at the absolute resolution limits (vessels $\le 2$ pixels wide). DeepLabV3+ occasionally exhibits fragmentation; it correctly identifies the presence of the capillary but predicts a discontinuous, dashed line. This fragmentation occurs because the spatial down-sampling in the encoder (even with atrous convolutions) fundamentally loses some spatial information that the single up-sampling pass in the decoder struggles to fully reconstruct.

In contrast, the dense skip connections of UNet++ demonstrate superior performance in maintaining the continuity of these ultra-thin structures. By constantly shuttling high-resolution spatial features directly from the early encoder layers to the final decoder block, UNet++ bridges the spatial gaps that cause fragmentation in DeepLabV3+. 

\subsection{Handling Pathological Noise and Low Contrast}
Both models demonstrated excellent robustness against uneven background illumination, a direct consequence of the aggressive brightness and contrast augmentation applied via Albumentations during training. In regions where the contrast agent density drops significantly, causing the vessel to visually fade into the tissue background, the models correctly leverage the broader contextual layout (the direction and curvature of the vessel leading into the low-contrast zone) to infer the presence of the vessel, resulting in continuous segmentations where traditional thresholding would inevitably fail.

\begin{figure}[htbp]
\centering
\includegraphics[width=\textwidth]{images/single_prediction_501.png}
\caption{Qualitative comparison of deep learning segmentations on test image 501. Left: Original RGB image showing the vascular network on the chick embryo CAM. Middle: Expert-annotated manual ground-truth mask. Right: Automated prediction mask generated by the trained UNet++ architecture showing high spatial fidelity and thin capillary continuity.}
\label{fig:single_prediction_501}
\end{figure}


\section{Morphological Topological Biomarkers (Skeletonization)}
The culmination of this thesis pipeline is the automated extraction of quantitative morphological biomarkers from the raw pixel segmentations. This is achieved through the downstream application of mathematical skeletonization algorithms to the binary masks predicted by the deep learning models.

\subsection{Extraction of Distinct Vessel Networks}
Following the binarization of the probability maps, the Zhang-Suen thinning algorithm reduces the complex, varying-width vascular tree into a strictly 1-pixel wide topological skeleton. By running a Connected Component Labeling algorithm on this skeleton, the system autonomously counts the number of distinct, structurally isolated vascular networks present in the angiogram.

Analysis of the generated \texttt{vessel\_counts.csv} report for the testing set reveals highly consistent biological structures. For example, Image ID 501 yielded 15 distinct connected networks, Image ID 502 yielded 19 networks, and Image ID 506 yielded 12 networks. Across a random sample of 10 test images (IDs 501-510), the pipeline detected an average of $15.9$ distinct networks per image. 

The variation in the number of distinct networks provides immediate clinical insight. A significantly elevated count of distinct networks in a single localized region could be a strong indicator of pathological neovascularization (the uncontrolled proliferation of fragile new blood vessels, a hallmark of severe Diabetic Retinopathy). Conversely, a severely reduced network count might indicate localized ischemia or vascular occlusion. By automating this count, the proposed pipeline transitions from a simple image processing tool to a diagnostic assistant.

\subsection{Branch Points and End Points}
The skeletonization pipeline further analyzes the connectivity of the 1-pixel thick graph to identify critical topological nodes: branch points (bifurcations) and end points (terminations). 

The \texttt{vessel\_counts.csv} data highlights the extreme density of the vascular network. Image ID 501 contains 3,211 distinct branch points, while Image ID 506 contains 3,640 branch points. The ratio of branch points to end points (e.g., 3,211 branch points to 59 end points in Image 501) numerically describes the extreme interconnectivity and looping nature of the vascular tree. 

These massive, precise nodal counts are physically impossible for a human clinician to extract manually. The ability of the proposed UNet++/DeepLabV3+ pipeline to first cleanly segment the network, and the subsequent skeletonization algorithm to autonomously extract thousands of precise topological nodes in mere seconds, definitively answers Research Question 3 (RQ3) and establishes the high clinical value of the integrated methodology.

\subsection{Topological Validation via Pearson Correlation}
To rigorously validate that the morphological features extracted from the automated segmentations accurately represent the true anatomical topology, a Pearson correlation analysis was conducted. We compared the skeleton properties (Connected Components, Branch Points, and Endpoints) extracted from the manual expert ground-truth (GT) masks against the properties extracted from predictions of both the UNet++ and DeepLabV3+ architectures across a subset of 10 independent test images (IDs 501–510).

The Pearson correlation coefficient ($r$) and its corresponding statistical significance ($p$-value) were calculated for each metric. The summary of these correlation metrics is detailed in Table~\ref{tab:pearson_correlation_summary}.

\begin{table}[h]
\centering
\caption{Pearson Correlation Summary ($r$-value and $p$-value) comparing Ground Truth and Predicted Skeleton Properties ($n = 137$ Images)}
\label{tab:pearson_correlation_summary}
\begin{tabular}{@{}lcccc@{}}
\toprule
\textbf{Skeleton Property} & \multicolumn{2}{c}{\textbf{UNet++}} & \multicolumn{2}{c}{\textbf{DeepLabV3+}} \\
\cmidrule(lr){2-3} \cmidrule(lr){4-5}
 & \textbf{$r$-value} & \textbf{$p$-value} & \textbf{$r$-value} & \textbf{$p$-value} \\ \midrule
Connected Components & \textbf{0.5267} & \textbf{< 0.0001} & \textbf{0.2788} & \textbf{0.0010} \\
Branch Points & \textbf{0.4049} & \textbf{< 0.0001} & -0.0459 & 0.5941 \\
Endpoints & \textbf{0.6704} & \textbf{< 0.0001} & \textbf{0.3817} & \textbf{< 0.0001} \\
\bottomrule
\end{tabular}
\end{table}

As summarized in Table~\ref{tab:pearson_correlation_summary}, the complete test-set analysis ($n = 137$ images) demonstrates that the \textbf{UNet++} architecture yields automated topological features that are highly statistically significant ($p < 0.0001$) and strongly correlated with the manual ground-truth (GT) annotations across all three metrics. Conversely, \textbf{DeepLabV3+} demonstrates much weaker performance, failing to achieve statistical significance for vascular branching complexity ($r = -0.0459$, $p = 0.5941$).

Specifically, for capillary dead-ends (Endpoints), UNet++ achieves a strong correlation of $r = 0.6704$ ($p < 0.0001$), outperforming DeepLabV3+ ($r = 0.3817$, $p < 0.0001$). For distinct vessel networks (Connected Components), UNet++ demonstrates a moderate correlation of $r = 0.5267$ ($p < 0.0001$), compared to $r = 0.2788$ ($p = 0.0010$) for DeepLabV3+. For branch points, UNet++ maintains a significant correlation of $r = 0.4049$ ($p < 0.0001$). 

This head-to-head comparison provides strong mathematical evidence that UNet++ preserves vascular topology far better than DeepLabV3+. The failure of DeepLabV3+ to capture branching complexity is attributed to the lack of dense skip connections in its decoder; without early high-resolution spatial feature maps, the ASPP module occasionally creates discontinuous prediction segments in fine capillary regions. When skeletonized, these discontinuities split single branching structures into multiple disconnected components and spurious ends, disrupting the overall branching metrics. UNet++ successfully aggregates multi-scale encoder features to prevent this topological degradation.

The scatter plots comparing the ground-truth and predicted topological metrics for individual images are shown in Figure~\ref{fig:pearson_correlation_plots}.

\begin{figure}[htbp]
\centering
\includegraphics[width=\textwidth]{images/pearson_correlation_plots.png}
\caption{Pearson correlation scatter plots comparing manual ground-truth (GT) skeleton properties against automated predictions from UNet++ (top row) and DeepLabV3+ (bottom row) across test images 501–510. The dashed lines show the linear regression fits.}
\label{fig:pearson_correlation_plots}
\end{figure}

\section{CAM Assay: Tumor Angiogenesis Assessment}
The primary biological application of the developed deep learning and skeletonization pipeline is to perform a quantitative assessment of angiogenic potential using the Chorioallantoic Membrane (CAM) assay on fertilized egg embryos. In cancer biology, tumor angiogenesis---the process by which tumors recruit new blood vessels to sustain their growth---is a hallmark of malignant progression. The CAM of the chick embryo provides an established \textit{in vivo} model system for evaluating pro-angiogenic and anti-angiogenic responses, as its dense vascular network closely resembles the microvascular environment of solid tumors.

Four experimental groups were established: three treatment concentrations of the compound dissolved in 0.9\% normal saline (0.1\,$\mu$g, 1\,$\mu$g, and 10\,$\mu$g) and a vehicle control consisting of 0.9\% saline alone. Vascular morphology was assessed at five timepoints (0h, 2h, 4h, 8h, 24h). Each of the 637 cropped images was matched back to its original raw acquisition folder using downscaled Mean Squared Error (MSE) matching ($\text{MSE} < 200$) to assign concentration and timepoint metadata. The analysis was conducted at the biological replicate level: images were deduplicated to one entry per unique raw image, grouped by individual egg, and only eggs possessing a baseline (0h) measurement were retained. The trained UNet++ model segmented the vascular networks, and the morphological thinning pipeline extracted three primary topological metrics. The aggregated statistics are detailed in Table~\ref{tab:egg_embryo_growth}.

\begin{table}[htbp]
\centering
\caption{CAM Assay: Quantitative Vascular Morphology Across Drug Concentrations and Time (Biological Replicate Level)}
\label{tab:egg_embryo_growth}
\small
\begin{tabular}{lccccc}
\toprule
\textbf{Concentration} & \textbf{Time} & \textbf{n (eggs)} & \textbf{Networks (Mean $\pm$ SD)} & \textbf{Branch Points (Mean $\pm$ SD)} & \textbf{End Points (Mean $\pm$ SD)} \\ \midrule
0.1\,$\mu$g & 0h & 8 & 13.38 $\pm$ 5.12 & 2824.75 $\pm$ 739.74 & 51.00 $\pm$ 6.93 \\
 & 2h & 5 & 13.60 $\pm$ 2.65 & 2298.80 $\pm$ 395.97 & 44.60 $\pm$ 6.05 \\
 & 4h & 4 & 14.50 $\pm$ 4.72 & 2502.25 $\pm$ 773.55 & 53.00 $\pm$ 1.41 \\
 & 8h & 3 & 11.33 $\pm$ 1.25 & 2506.00 $\pm$ 733.87 & 44.00 $\pm$ 2.94 \\
 & 24h & 1 & 4.00 $\pm$ 0.00 & 5115.00 $\pm$ 0.00 & 53.00 $\pm$ 0.00 \\ \midrule
1\,$\mu$g & 0h & 17 & 9.53 $\pm$ 4.34 & 2906.47 $\pm$ 989.39 & 41.24 $\pm$ 10.42 \\
 & 2h & 12 & 10.83 $\pm$ 3.53 & 2859.67 $\pm$ 986.11 & 47.75 $\pm$ 7.34 \\
 & 4h & 10 & 12.30 $\pm$ 3.74 & 2458.00 $\pm$ 1021.17 & 44.40 $\pm$ 6.71 \\
 & 8h & 8 & 11.25 $\pm$ 3.34 & 2450.38 $\pm$ 842.00 & 39.12 $\pm$ 7.99 \\
 & 24h & 3 & 12.67 $\pm$ 3.68 & 2461.33 $\pm$ 509.16 & 51.00 $\pm$ 4.32 \\ \midrule
10\,$\mu$g & 0h & 12 & 12.00 $\pm$ 4.00 & 2765.33 $\pm$ 1442.68 & 44.25 $\pm$ 13.50 \\
 & 2h & 9 & 10.33 $\pm$ 3.56 & 3161.67 $\pm$ 1390.59 & 46.67 $\pm$ 10.62 \\
 & 4h & 7 & 11.14 $\pm$ 3.14 & 2560.86 $\pm$ 662.14 & 47.71 $\pm$ 10.28 \\
 & 8h & 6 & 16.33 $\pm$ 3.77 & 2626.67 $\pm$ 632.51 & 48.67 $\pm$ 9.39 \\
 & 24h & 1 & 18.00 $\pm$ 0.00 & 1523.00 $\pm$ 0.00 & 59.00 $\pm$ 0.00 \\ \midrule
Control (saline) & 0h & 7 & 11.14 $\pm$ 3.94 & 2207.29 $\pm$ 849.19 & 44.14 $\pm$ 10.26 \\
 & 2h & 3 & 13.67 $\pm$ 0.94 & 2345.33 $\pm$ 793.97 & 44.33 $\pm$ 4.78 \\
 & 4h & 3 & 10.00 $\pm$ 4.32 & 2222.33 $\pm$ 230.91 & 46.00 $\pm$ 12.68 \\
 & 8h & 3 & 14.67 $\pm$ 1.70 & 2931.67 $\pm$ 473.54 & 64.33 $\pm$ 11.15 \\
 & 24h & 2 & 16.00 $\pm$ 2.00 & 2545.50 $\pm$ 95.50 & 48.50 $\pm$ 3.50 \\
\bottomrule
\end{tabular}
\end{table}

The corresponding growth dynamics curves are visualized in Figure~\ref{fig:egg_embryo_curves}, mapping vessel network fragmentation (a proxy for anti-angiogenic activity), vascular branching complexity (a proxy for pro-angiogenic sprouting), and capillary dead-end accumulation (a proxy for vascular immaturity) over time.

\begin{figure}[htbp]
\centering
\includegraphics[width=\textwidth]{images/vessel_growth_plots.png}
\caption{CAM Assay: Tumor Angiogenesis Assessment across drug concentrations and time. Left: vessel network fragmentation ($\uparrow$ = anti-angiogenic). Middle: vascular branching complexity ($\uparrow$ = pro-angiogenic). Right: capillary dead-ends ($\uparrow$ = immature vasculature).}
\label{fig:egg_embryo_curves}
\end{figure}

\subsection{Vessel Network Fragmentation and Tumor Blood Supply}
The count of distinct, disconnected vessel networks serves as a direct indicator of vascular connectivity. In the context of tumor angiogenesis, a \textit{decreasing} network count signifies that isolated vessel fragments are fusing (anastomosis) into a well-connected circulatory web---a pro-angiogenic response that would provide a tumor with an efficient blood supply. Conversely, an \textit{increasing} count indicates vascular fragmentation---an anti-angiogenic response that would starve a tumor of oxygen and nutrients.

As shown in Figure~\ref{fig:egg_embryo_curves} (left panel), the 0.1\,$\mu$g group exhibits a striking consolidation from $13.38 \pm 5.12$ networks at baseline to just $4.00$ at 24h, indicating a strongly \textbf{pro-angiogenic} response. This level of vascular consolidation would create an ideal, tumor-favorable microenvironment with maximum blood perfusion. The 1\,$\mu$g group shows a stable, regulated pattern fluctuating narrowly between $9.53$ and $12.67$ networks, suggesting a \textbf{neutral} effect on vascular connectivity. In stark contrast, the 10\,$\mu$g group displays progressive vascular fragmentation, with the network count rising sharply from $12.00 \pm 4.00$ at baseline to $18.00$ at 24h. This \textbf{anti-angiogenic} response---where existing vessel connections are actively disrupted---would severely limit a tumor's ability to establish functional blood supply. The vehicle control (0.9\% saline) trends upward from $11.14 \pm 3.94$ to $16.00 \pm 2.00$ with erratic oscillations, confirming that the observed consolidation at 0.1\,$\mu$g and fragmentation at 10\,$\mu$g are compound-mediated effects rather than natural developmental artifacts.

\subsection{Vascular Branching Complexity and Tumor Capillary Density}
Branch points (bifurcations) quantify the density and complexity of the capillary network. In tumor biology, a dense, highly branched vascular bed maximizes nutrient extraction and provides multiple routes for cancer cell intravasation (entry into the bloodstream), facilitating metastasis. Conversely, a simplified vasculature with fewer branches reduces the tumor's access to systemic circulation.

As visualized in the middle panel of Figure~\ref{fig:egg_embryo_curves}, the 0.1\,$\mu$g group sustains moderate branching ($2824.75 \pm 739.74$ at 0h) and culminates in a dramatic spike to $5115.00$ branch points at 24h---nearly double any other group. This massive branching, combined with the network consolidation observed in the previous metric, indicates a strongly \textbf{pro-angiogenic} response that produces a dense, well-connected capillary bed---precisely the vascular infrastructure that tumors actively induce through VEGF secretion. The 1\,$\mu$g group shows a mild pruning effect, declining from $2906.47 \pm 989.39$ to $\sim$2450--2460 branch points, indicating a moderate reduction in vascular density. Most critically, the 10\,$\mu$g group exhibits a progressive collapse in branching complexity from $2765.33 \pm 1442.68$ to $1523.00$ at 24h---a reduction of nearly 45\%. Combined with the vascular fragmentation observed in the network count, this demonstrates that the high dose is \textbf{dismantling the vascular architecture at every topological level}, effectively creating a hostile microenvironment for tumor growth.

\subsection{Capillary Dead-Ends and Vascular Maturity}
End points represent blind-ending capillary tips---vessel termini that fail to connect to other vessels to form functional blood-carrying loops. In the context of tumor perfusion, a vessel that terminates without anastomosing is structurally present but functionally inert: blood enters but cannot circulate. An increasing endpoint count therefore indicates the accumulation of immature, non-perfusable vasculature.

The 0.1\,$\mu$g group maintains a remarkably stable endpoint count ($44$--$53$) despite its massive increase in branching complexity. This critical observation indicates that nearly every new branch successfully connects to another vessel to form a closed perfusion loop---the \textbf{most tumor-favorable outcome}, as it creates a dense vascular network in which blood can actually circulate. The 10\,$\mu$g group shows a steady upward trend from $44.25 \pm 13.50$ to $59.00$, confirming that the remaining vessels are increasingly \textbf{immature and non-functional}---broken stumps from progressive vascular destruction rather than active growing tips. The vehicle control displays a pronounced transient spike to $64.33 \pm 11.15$ at 8h before recovering to $48.50$ by 24h; this spike represents a natural developmental burst of new sprouts that subsequently connect, in contrast to the 10\,$\mu$g group's irreversible accumulation of dead-ends.

Taken together, the three morphological metrics reveal a clear \textbf{biphasic dose-response}: the low dose (0.1\,$\mu$g) is strongly pro-angiogenic, creating a consolidated, densely branched, and functionally perfusable vascular network that would support tumor growth; the medium dose (1\,$\mu$g) has a neutral to mildly anti-proliferative effect; and the high dose (10\,$\mu$g) is anti-angiogenic, producing a fragmented, simplified, and non-functional vasculature that would starve a tumor of blood supply. This biphasic pattern is consistent with the pharmacological principle of hormesis, where the same compound exhibits opposing biological effects at different concentrations.

\section{Limitations of the Proposed Methodology}
While the quantitative and qualitative results are highly promising, a rigorous academic analysis requires an explicit acknowledgment of the methodology's current limitations.

The primary limitation involves the binarization thresholding step prior to skeletonization. We currently utilize a static threshold ($\tau = 0.5$). While highly effective for the confident predictions of the primary vessels, this static threshold can inadvertently break the continuity of ultra-thin capillaries whose predicted probabilities hover closely around $0.45 - 0.55$. When a continuous vessel is thresholded into a dashed line, the skeletonization algorithm falsely interprets each dash as a distinct network, artificially inflating the "Connected Networks Count" and introducing spurious "End Points." 

Future iterations of this pipeline must investigate adaptive, hysteresis-based thresholding algorithms (similar to the Canny edge detector logic) that track the probability ridge-lines to preserve the connectivity of low-confidence micro-capillaries prior to morphological thinning.

\section{Statistical Validation: Spearman Correlation and Paired t-Tests}
To provide additional statistical rigor beyond the Pearson analysis, two complementary statistical methods were employed.

\subsection{Spearman Rank Correlation}
While the Pearson correlation coefficient measures linear relationships, the Spearman rank correlation coefficient ($\rho$) assesses monotonic relationships without assuming normality of the underlying data distribution. This is particularly important for morphological counts (e.g., branch points, connected components), which may not follow a Gaussian distribution across the test set.

The Spearman correlation heatmaps (Figure~\ref{fig:spearman_heatmaps}) confirm the directional consistency of the Pearson analysis: UNet++ predictions maintain strong monotonic agreement with ground-truth topological features, while DeepLabV3+ exhibits weaker rank-order preservation, particularly for branch point counts.

\begin{figure}[H]
\centering
\includegraphics[width=0.92\textwidth]{images/spearman_correlation_heatmaps.png}
\caption{Spearman rank correlation heatmaps comparing ground-truth and predicted skeleton properties for UNet++ and DeepLabV3+. Warm colors indicate strong positive monotonic correlation. The results confirm the superiority of UNet++ in preserving vascular topology.}
\label{fig:spearman_heatmaps}
\end{figure}

\subsection{Paired Statistical Testing}
To test whether the differences in extracted morphological features between the ground truth and the model predictions are statistically significant, paired $t$-tests were conducted for each metric. The null hypothesis ($H_0$) states that there is no significant difference between the ground-truth and the predicted morphological counts.

The box plot distributions (Figure~\ref{fig:ttest_boxplots}) visually illustrate the spread and central tendency of the predicted versus ground-truth morphological features, providing an intuitive understanding of the prediction accuracy and variance for both architectures.

\begin{figure}[H]
\centering
\includegraphics[width=0.92\textwidth]{images/statistical_ttest_boxplots.png}
\caption{Box plot distributions comparing ground-truth and model-predicted morphological features (Connected Components, Branch Points, Endpoints) for both UNet++ and DeepLabV3+. The plots illustrate the distributional overlap between ground-truth annotations and automated predictions, with UNet++ demonstrating closer distributional alignment.}
\label{fig:ttest_boxplots}
\end{figure}
